/**
 * The `@modal` parallel-route slot renders nothing on a normal navigation —
 * only an intercepted route (e.g. `(.)item/[slug]`) fills it in.
 */
export default function Default() {
  return null
}
